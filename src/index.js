function sayHi(name) {
  return `Hello ${name}`
}
// This is a simple function that takes a name as an argument and returns a greeting message.
// It can be used in various applications to greet users or display messages.
module.exports = sayHi
